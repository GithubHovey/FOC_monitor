# FOC电机控制上位机需求文档

## 1. 项目概述

本项目旨在开发一个功能完整的FOC（Field-Oriented Control）电机控制上位机软件，用于实时监控和控制FOC电机，支持多种控制模式和数据可视化。

## 2. 功能需求

### 2.1 实时数据监控
- **时域曲线图显示**：实时绘制电机参数的时域曲线
- **监控参数**（所有参数均为可选项，可随时开启/关闭显示）：
  - **电流参数**：
    - 三相电流（Ia, Ib, Ic）
  - **电压参数**：
    - 目标Q轴电压（Vq_ref）
    - 目标D轴电压（Vd_ref）
    - 实际Q轴电压（Vq）
    - 实际D轴电压（Vd）
  - **转速参数**：
    - 目标转速（RPM_ref）
    - 当前转速（RPM）
  - **位置参数**：
    - 目标位置（Position_ref）
    - 当前位置（Position）


### 2.2 控制模式

#### 2.2.1 速度模式
- 速度设定值输入
- 速度环PID参数调节
- 速度跟随性能显示

#### 2.2.2 位置模式
- 位置设定值输入
- 位置环PID参数调节
- 位置跟踪精度显示

#### 2.2.3 力矩模式
- 转矩设定值输入
- 转矩环PID参数调节
- 力矩控制精度显示

### 2.3 调试功能
- **零位校准**：电机零位自动/手动校准功能
- **参数保存/加载**：配置文件管理
- **实时参数调整**：运行时PID参数调整

### 2.4 通信配置
- **CAN ID修改**：支持电机CAN节点ID配置
- **串口配置**：波特率、数据位、停止位、校验位设置
- **通信协议**：自定义二进制协议或MODBUS协议

### 2.5 数据管理
- **数据记录**：实时数据保存为CSV格式
- **数据回放**：历史数据加载和回放
- **报表生成**：测试报告自动生成

## 3. 技术规格

### 3.1 硬件接口
- **通信方式**：串口（RS232/RS485/USB转串口）
- **可选扩展**：CAN总线接口
- **采样频率**：≥1kHz

### 3.2 软件要求
- **跨平台支持**：Windows, Linux
- **实时性**：数据刷新率 ≥ 50Hz
- **数据精度**：32位浮点数精度

### 3.3 性能指标
- **最大通道数**：支持多电机同时监控（最多8个）
- **数据延迟**：< 100ms
- **存储容量**：支持长时间数据记录（24小时以上）

## 4. 系统架构

### 4.1 软件架构
```
┌─────────────────┐
│   用户界面层     │
│  - 数据可视化    │
│  - 控制面板      │
│  - 参数配置      │
└─────────────────┘
          │
┌─────────────────┐
│   业务逻辑层     │
│  - 数据处理      │
│  - 控制算法      │
│  - 通信协议      │
└─────────────────┘
          │
┌─────────────────┐
│   硬件接口层     │
│  - 串口通信      │
│  - CAN通信       │
│  - 数据采集      │
└─────────────────┘
```

### 4.2 通信协议设计

#### 4.2.1 数据帧格式
```
[头字节][长度][命令字][数据域][校验][尾字节]
```

#### 4.2.2 主要命令字
- 0x01: 读取电机参数
- 0x02: 设置控制模式
- 0x03: 设置目标值
- 0x04: 参数校准
- 0x05: CAN ID配置

## 5. 用户界面设计

### 5.1 主界面布局
```
┌────────────────────────────────────────┐
│ 菜单栏：文件 视图 设置 帮助            │
├────────────────────────────────────────┤
│ 左侧：设备树、参数列表                 │
├────────────────────────────────────────┤
│ 中部：实时曲线图（多标签页）           │
├────────────────────────────────────────┤
│ 右侧：控制面板、参数调节               │
├────────────────────────────────────────┤
│ 底部：状态栏、通信状态、数据统计       │
└────────────────────────────────────────┘
```

### 5.2 曲线图功能
- 多曲线同屏显示
- 缩放和平移操作
- 数据游标测量
- 自动标尺调整
- 曲线颜色自定义

## 6. 开发技术栈

### 6.1 技术方案

#### HTML/CSS/JavaScript + Electron + Node.js
- **前端技术**：
  - HTML5 + CSS3 构建用户界面
  - JavaScript (ES6+) 实现交互逻辑
  - 现代图表库：Chart.js（推荐）或 ECharts 用于实时数据可视化
  - Web Components 或 React/Vue 框架（可选）

- **后端技术**：
  - Electron 框架提供跨平台桌面应用环境
  - Node.js 运行时环境
  - serialport 库处理串口通信
  - 自定义二进制协议解析模块
  - Worker 线程处理实时数据采集和处理

- **技术优势**：
  - 完全基于Web技术栈，开发效率高
  - 丰富的UI组件和图表库生态系统
  - 良好的跨平台支持（Windows/Linux）
  - 现代化的开发工具和调试环境
  - 易于维护和功能扩展



### 6.3 数据持久化
- **配置文件**：JSON格式
- **数据存储**：SQLite数据库
- **报表格式**：PDF/HTML

## 7. 项目目录结构

### 7.1 模块化目录设计

```
FOC_Monitor/
├── src/                          # 源代码目录
│   ├── frontend/                 # 前端代码
│   │   ├── components/           # 可复用组件
│   │   ├── pages/               # 页面组件
│   │   ├── assets/              # 静态资源
│   │   ├── styles/              # 样式文件
│   │   └── utils/               # 工具函数
│   └── backend/                 # 后端代码
│       ├── communication/       # 通信模块
│       ├── data-processing/     # 数据处理
│       ├── control-algorithms/  # 控制算法
│       └── utils/               # 工具模块
├── dist/                        # 产物目录（构建输出）
│   ├── win/                     # Windows版本
│   ├── linux/                   # Linux版本
│   └── mac/                     # macOS版本
├── assets/                      # 素材目录
│   ├── icons/                   # 图标资源
│   ├── images/                  # 图片资源
│   ├── fonts/                   # 字体文件
│   └── docs/                    # 文档素材
├── build/                       # 生成目录（编译中间文件）
│   ├── electron/                # Electron构建文件
│   ├── webpack/                 # Webpack构建文件
│   └── temp/                    # 临时文件
├── config/                      # 配置文件
├── scripts/                     # 脚本文件
└── docs/                        # 项目文档
```

### 7.2 目录说明

#### 前端代码 (src/frontend)
- **components/**：可复用的React/Vue组件
- **pages/**：主要页面组件（监控页、配置页、历史数据页等）
- **assets/**：静态资源（图片、字体等）
- **styles/**：CSS/SCSS样式文件
- **utils/**：工具函数和辅助类

#### 后端代码 (src/backend)  
- **communication/**：串口通信、协议解析模块
- **data-processing/**：实时数据处理和算法
- **control-algorithms/**：FOC控制算法实现
- **utils/**：后端工具函数

#### 产物目录 (dist)
- **win/linux/mac**：各平台特定的打包输出文件
- 虽然Electron应用代码是跨平台的，但最终的可执行文件需要针对不同平台进行打包
- 每个平台目录包含该平台的原生可执行文件和依赖库

#### 素材目录 (assets)
- **icons/**：应用程序图标
- **images/**：界面图片资源
- **fonts/**：字体文件
- **docs/**：用户手册、帮助文档

#### 生成目录 (build)
- **electron/**：Electron构建中间文件
- **webpack/**：Webpack构建输出
- **temp/**：编译临时文件

## 8. 项目实施计划

### 8.1 开发阶段
1. **需求分析和设计**（1周）
2. **通信协议开发**（2周）
3. **核心功能开发**（4周）
4. **用户界面开发**（3周）
5. **测试和优化**（2周）

### 7.2 测试要求
- 单元测试覆盖率 ≥ 80%
- 跨平台兼容性测试
- 性能压力测试
- 长时间稳定性测试

## 8. 质量要求

### 8.1 可靠性
- 7×24小时连续运行
- 异常自动恢复
- 数据完整性保证

### 8.2 安全性
- 参数范围校验
- 操作确认机制
- 数据备份功能

### 8.3 易用性
- 直观的操作界面
- 详细的使用文档
- 在线帮助系统

## 9. 交付物

1. 可执行程序（Windows/Linux版本）
2. 源代码和开发文档
3. 用户手册和技术文档
4. 测试报告和验收文档

## 10. 维护和支持

- 1年免费技术支持和维护
- 定期功能更新和bug修复
- 用户反馈收集和处理机制

---
*文档版本：v1.0*
*最后更新：2024年1月*
*创建者：FOC电机控制上位机开发团队*